import React from 'react'

const Test = (props) => {
  return (
    <div>
      <center>
        <h3>Name : {props.name} And  Age : {props.age}</h3>
      </center>
    </div>
  )
}

export default Test
